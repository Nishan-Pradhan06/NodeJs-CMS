<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Delete Confirmation</title>
        <style>
        /* Reset some default styles for the page */
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Style for the popup delete card */
        .popup-card {
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
            max-width: 400px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        /* Style for the button */
        .delete-button {
            background-color: #f44336;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
            margin-right: 10px;
        }

        .cancel-button {
            background-color: #4CAF50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .delete-button:hover {
            background-color: #d32f2f;
        }

        .cancel-button:hover {
            background-color: #45a049;
        }
    </style>
    </head>
    <body>
        <div class="popup-card">
            <h1>Delete Confirmation</h1>
            <p>Are you sure you want to delete this item?</p>
            <a href="/"><button class="delete-button" onclick="deleteItem()">Delete</button></a>

            <a href="/singleblog"><button class="cancel-button"
                    onclick="cancelDelete()">Cancel</button></a>
        </div>

        <script>
        // function deleteItem() {
        //     alert("Item deleted successfully!");
        // }

        // function cancelDelete() {
        //     alert("Delete operation canceled.");
        // }
    </script>
    </body>
</html>
